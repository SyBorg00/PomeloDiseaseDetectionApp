import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#78B16D",
    secondary: "#FFFFFF",
    background: "#FFFFFF",
  },
};

export default theme;