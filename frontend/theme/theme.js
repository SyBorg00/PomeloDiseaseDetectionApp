import { DefaultTheme } from "react-native-paper";

export default theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#78B16D",
        secondary: "#FFFFFF",
        background: "#FFFFFF"
    }
}